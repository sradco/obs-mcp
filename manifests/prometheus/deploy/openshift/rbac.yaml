apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: obs-mcp-monitoring-reader
rules:
  # Required for Prometheus API access with kubeconfig auth mode
  # 'create' is needed because Prometheus uses HTTP POST for some query endpoints
  # and the Kubernetes API proxy maps POST to 'create'.
  - apiGroups: ["monitoring.coreos.com"]
    resources: ["prometheuses/api"]
    resourceNames: ["k8s"]
    verbs: ["get", "create"]
  # Required for Alertmanager API access (read-only). Silence writes and
  # AlertRelabelConfig (platform drop/restore) need extra Roles bound to the
  # caller; e2e tests grant those for the obs-mcp service account.
  - apiGroups: ["monitoring.coreos.com"]
    resources: ["alertmanagers/api"]
    resourceNames: ["main"]
    verbs: ["get", "list"]
---
# RoleBinding scoped to openshift-monitoring namespace only
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: obs-mcp-monitoring-reader
  namespace: openshift-monitoring
subjects:
  - kind: ServiceAccount
    name: obs-mcp
    namespace: obs-mcp
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: obs-mcp-monitoring-reader
